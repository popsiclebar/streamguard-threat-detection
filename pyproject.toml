[project]
name = "streamguard-threat-detection"
version = "0.1.0"
description = "Local-first AI threat detection pipeline MVP"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
testpaths = ["tests"]
