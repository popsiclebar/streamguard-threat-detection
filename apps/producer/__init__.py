"""Event producer application package for StreamGuard.

The producer replays local JSONL security-event samples into a Kafka-compatible
topic so the future detector worker can consume a realistic event stream.
"""
